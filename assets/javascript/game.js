//Variables to log all the user wins, losses, current score, game restart, and button images.
var win = 0;
var lose = 0;
var userCurrentScore= 0;
/*var images =["assets/images/purple gem.png",
            "assets/images/black.png",
            "assets/images/blue gem.png",
            "assets/images/Multicolor_Diamonds_Set_PNG_Clipart.png"];*/
var gameRestart = function() {
    randomComGenResult = Math.floor(Math.random() * 102 + 19);
    randomCrystalNum = Math.floor(Math.random() * 11) +1;
    userCurrentScore = 0;
};

/////GAME START/////
$(document).ready(function() { 
    
//A random number between "19 & 120" generated by the computer as the result and number to "match"
randomComGenResult = Math.floor(Math.random() * 102 + 19);
console.log(randomComGenResult);
$('#comnum').text(randomComGenResult);
    
//A random number between "1 & 12" generated and assigned to each of the 4 crystals in a "for" loop
//Removed the for loop
//for (var i =0; i<4; i++) {
randomCrystalNum = Math.floor(Math.random() * 11) +1;
console.log(randomCrystalNum);

//I removed the dynamic part until I can really figure it out!!!!!
//crystalButtons =$("<button>");
//crystalButtons.attr({
//"class":'crystal',
// "id":randomCrystalNum,
//});
//$(".crystals").append(crystalButtons);  
//$(".button").text("<img="+ images[i] +">");
//});

//Giving each crystal the random generated number between "1 & 12"
var black =  Math.floor(Math.random() * 11) +1;
var purple =  Math.floor(Math.random() * 11) +1;
var pink =  Math.floor(Math.random() * 11) +1;
var blue =  Math.floor(Math.random() * 11) +1;



//A function for evrey crytal to raise the score and sum up the current score 

$('#purple-crystal').on('click', function() {
    userCurrentScore = userCurrentScore + purple;
    console.log("userCurrentScore= " + userCurrentScore);
    $('#usernum').html(userCurrentScore);
// Justifies a won game
if (randomComGenResult === userCurrentScore) {
    win++;
    $('.leftWin').html(win);
    alert("Winner Winner Chicken Dinner!!!")
    gameRestart();
}
 //Justifies a lost game
else if (randomComGenResult < userCurrentScore) {
    lose--;
    $('.rightLose').html(lose);
    gameRestart()
};
});

///////////////////////////////////////////////////////////

$('#black-crystal').on('click', function() {
    userCurrentScore = userCurrentScore + black;
    console.log("userCurrentScore= " + userCurrentScore);
    $('#usernum').html(userCurrentScore);
// Justifies a won game
if (randomComGenResult === userCurrentScore) {
    win++;
    $('.leftWin').html(win);
    alert("Winner Winner Chicken Dinner!!!")
    gameRestart();
}
 //Justifies a lost game
else if (randomComGenResult < userCurrentScore) {
    lose--;
    $('.rightLose').html(lose);
    gameRestart()
}
});

///////////////////////////////////////////////////////////

//A function for evrey crytal to raise the score and sum up the current score 

$('#blue-crystal').on('click', function() {
    userCurrentScore = userCurrentScore + blue;
    console.log("userCurrentScore= " + userCurrentScore);
    $('#usernum').html(userCurrentScore);
// Justifies a won game
if (randomComGenResult === userCurrentScore) {
    win++;
    $('.leftWin').html(win);
    alert("Winner Winner Chicken Dinner!!!")
    gameRestart();
}
 //Justifies a lost game
else if (randomComGenResult < userCurrentScore) {
    lose--;
    $('.rightLose').html(lose);
    gameRestart()
}
});

///////////////////////////////////////////////////////////

//A function for evrey crytal to raise the score and sum up the current score 

$('#pink-crystal').on('click', function() {
    userCurrentScore = userCurrentScore + pink;
    console.log("userCurrentScore= " + userCurrentScore);
    $('#usernum').html(userCurrentScore);
// Justifies a won game
if (randomComGenResult === userCurrentScore) {
    win++;
    $('.leftWin').html(win);
    alert("Winner Winner Chicken Dinner!!!")
    gameRestart();
}
 //Justifies a lost game
else if (randomComGenResult < userCurrentScore) {
    lose--;
    $('.rightLose').html(lose);
    gameRestart()
}
});
});