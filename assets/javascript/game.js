
//Variables to log all the user wins, losses, current score, and game restart
var win = 0;
var lose = 0;
var userCurrentScore;
var previous = 0;
var gameRestart = function() {
    randomGenComResult = Math.floor(Math.random() * 102 + 19);
    randomCrystalNum = Math.floor(Math.random() * 11) +1;
    userCurrentScore = 0;
//};
/////GAME START/////
$(document).ready(function() { 
    
    //A random number between "19 & 120" generated by the computer as the result and number to "match"
    randomComGenResult = Math.floor(Math.random() * 102 + 19);
    console.log(randomComGenResult);
    $('#comnum').text(randomComGenResult);
    
    //A random number between "1 & 12" generated and assigned to each of the 4 crystals in a "for" loop
    for (var i =0; i<4; i++) {
    randomCrystalNum = Math.floor(Math.random() * 11) +1;
    console.log(randomCrystalNum);



        if (randomGenComResult === userCurrentScore) {
            win++;
            alert("Winner Winner Chicken Dinner!!!")
            gameRestart;
        }
        else if (randomGenComResult < userCurrentScore) {
            lose--;
            alert("You stink!!!")
            gameRestart;
        }
        else {
            userCurrentScore--;
        }

    });
});   







//That number should be genrated every single time we win or lose the game to each of the 4 crystals
//when clicking any crystal, it should be adding with the previous result until it equals the total score
//If it is greater than the random result, we lose and decrement the score with a lose counter
//if it is equal, we increment the score with a win counter